from simulate_basicsim_run_one import ex

## NOTE: This run uses the default parameter set (so not necessary to supply config options)

config_updates = {}  # No updates to supply
ex.run(config_updates=config_updates)
